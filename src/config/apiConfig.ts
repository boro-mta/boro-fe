const SERVER_URL = "http://localhost";
const SERVER_PORT = "5157";

const HEADERS = {
  "Content-Type": "application/json",
};

let apiConfig = {
  SERVER_URL,
  SERVER_PORT,
  HEADERS,
};

export default apiConfig;
